doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title koa
  body
    h2.title hello 
      if user.name
        span= user.name
      else
        span world

    //- 列表 
    ul.list
      - 
      each item in list
        li= item.label