doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title koa
  body
    h2.title hello 
      //- 条件语句
      if user.name
        span= user.name
      else
        span world

    
    ul.list
      - 
      //- 循环语句
      each item in list
        li= item.label